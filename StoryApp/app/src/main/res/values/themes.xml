<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StoryApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/yellow</item>
        <item name="colorSecondary">@color/blue</item>
        <item name="colorPrimaryContainer">@color/yellow_light</item>
        <item name="colorSecondaryContainer">@color/blue_dark</item>
        <item name="colorTertiary">@color/yellow</item>
        <item name="android:statusBarColor">@color/blue</item>
        <item name="android:navigationBarColor">@color/blue_dark</item>
    </style>

    <style name="Theme.StoryApp" parent="Base.Theme.StoryApp" />
</resources>